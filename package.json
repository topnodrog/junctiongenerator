{
  "name": "junction-generator-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/jgc-node && npm run build --workspace=packages/web",
    "test": "npm run test --workspace=packages/jgc-node",
    "typecheck": "npm run typecheck --workspace=packages/jgc-node && npm run build --workspace=packages/web"
  }
}
